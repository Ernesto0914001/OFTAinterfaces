function taller() {
    //Ingresamos un mensaje a mostrar
    var taller = prompt("Agregar Taller", "");
 console.log();

    //Detectamos si el usuario ingreso un valor
        if (taller != null)
    alert("Esta seguro que quiere agregar a :  " + taller);
}