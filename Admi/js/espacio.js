function espacio() {
    //Ingresamos un mensaje a mostrar
    var espacio = prompt("Agregar Espacio", "");
 console.log();

    //Detectamos si el usuario ingreso un valor
        if (espacio != null)
    alert("Esta seguro que quiere agregar a :  " + espacio);
}