function Docente() {
    //Ingresamos un mensaje a mostrar
    var Docente = prompt("Agregar Docente", "");
 console.log();

    //Detectamos si el usuario ingreso un valor
        if (Docente != null)
    alert("Esta seguro que quiere agregar a :  " + Docente);
}