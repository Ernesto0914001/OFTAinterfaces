function horario() {
    //Ingresamos un mensaje a mostrar
    var horario = prompt("Agregar Horario", "");
 console.log();

    //Detectamos si el usuario ingreso un valor
        if (horario != null)
    alert("Esta seguro que quiere agregar a :  " + horario);
}